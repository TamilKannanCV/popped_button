name: popped_button
description: "This package creates a 3d popped button purely in dart"
version: 0.0.1
repository: https://github.com/TamilKannanCV/popped_button.git

environment:
  sdk: ">=3.3.3 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      some_platform:
        pluginClass: somePluginClass
